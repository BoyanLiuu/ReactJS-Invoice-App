import styled from 'styled-components';

const FilterBarDiv = styled.div``;
const FilterBar = () => {
    return (
        <FilterBarDiv>
            <h1>Hello dude</h1>
        </FilterBarDiv>
    );
};
export default FilterBar;
